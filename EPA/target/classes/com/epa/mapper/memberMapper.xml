<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.epa.mapper.memberMapper">
    
    <select id="memberLogin" parameterType="com.epa.entity.epa"
        resultType="com.epa.entity.epa">
        select * from member where login_id=#{login_id} and login_id=#{login_id}
    </select>
    

    <insert id="memberJoin">
        INSERT INTO members(email, login_id, login_pw, nick, height, weight, birth, gender) VALUES 
        (#{email}, #{login_id}, #{login_pw}, #{nick}, #{height}, #{weight}, #{birth}, #{gender})
    </insert>
</mapper>